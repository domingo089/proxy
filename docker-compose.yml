traefik:
    image: traefik:v2.0
    command: --api.insecure=true --providers.docker
    ports:
      - 80:80
      - 443:443  
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /home/ubuntu/proyects/proxy/acme.json:/acme.json
      - /home/ubuntu/proyects/proxy/config.yml:/config.yml

    labels:
      - "traefik.http.routers.traefik.rule=Host(`traefik.rmastertv.com`)"
      - "traefik.http.services.traefik.loadbalancer.server.port=8080"
      - "traefik.http.middlewares.auth.basicauth.users=domingo08:{SHA}bHyjRfY/g1yzU/8VvWxeBS7Ajno="
      - "traefik.http.routers.apache.entrypoints=websecurity"
      - "traefik.http.routers.apache.tls.certresolver=myresolver"


  jenkins:
    image: jenkins/jenkins:lts
    volumes:
      - jenkins_home:/var/jenkins_home
    labels:
      - "traefik.http.routers.jenkins.rule=Host(`jenkins.rmastertv.com`)"
      - "traefik.http.services.jenkins.loadbalancer.server.port=8080"

  wpages-1:
    image: nginx:alpine
    volumes:
      - ../wpages/html:/usr/share/nginx/html
    labels:
      - "traefik.http.routers.wpages-1.rule=Host(`rmastertv.com`)"

  wpages-2:
    image: nginx:alpine
    volumes:
      - ../wpages-2/html:/usr/share/nginx/html
    labels:
      - "traefik.http.routers.wpages-2.rule=Host(`computacion.rmastertv.com`)"
  
  wpages-3:
    image: nginx:alpine
    volumes:
      - ../wpages-3/html:/usr/share/nginx/html
    labels:
    - "traefik.http.routers.wpages-3.rule=Host(`logistica.rmastertv.com`)"

volumes:
  jenkins_home: